cmake_minimum_required(VERSION 3.16)
project(cycle_tree_experiment LANGUAGES CXX)
enable_testing()
add_executable(test_cycle_tree ../../controller_manager/test/test_cycle_tree_standalone.cpp)
target_compile_features(test_cycle_tree PRIVATE cxx_std_17)
target_include_directories(test_cycle_tree PRIVATE ../../controller_manager/include)
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
  target_compile_options(test_cycle_tree PRIVATE -Wall -Wextra -Wpedantic -Werror)
endif()
add_test(NAME cycle_tree_contract COMMAND test_cycle_tree)
